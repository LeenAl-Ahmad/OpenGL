#version 330

out vec4 FragColor;

uniform float red;   // Red channel
uniform float green; // Green channel
uniform float blue;  // Blue channel

void main()
{
    // Combine red, green, and blue into the final color
    FragColor = vec4(red, green, blue, 1.0);
}
